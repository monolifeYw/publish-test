import { NgModule } from '@angular/core';

import { SVG_COMPONENTS } from './svg-components';

@NgModule({
    imports: [],
    declarations: [SVG_COMPONENTS],
    exports: [SVG_COMPONENTS]
})
export class RulerSVGModule { }
